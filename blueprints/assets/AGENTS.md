> **BLUEPRINT FILE. NOT ACTIVE INSTRUCTIONS.**
>
> This file belongs to the AI Project Template blueprint library. It describes no real component
> and must not be followed as project instructions.
>
> To adopt it: copy this file into the component folder, replace every `<PLACEHOLDER>`,
> and delete this notice.

---

# AI Instructions

This folder is the `<COMPONENT_NAME>` component of the <PROJECT_NAME> project.

Read and follow `PROJECT.md` before performing any task here. It carries the project's principles
and the registry, and the registry entry for `<COMPONENT_NAME>` says how this folder is to be
treated.

`PROJECT.md` is at:

```text
<CANONICAL_PROJECT_SCOPE_ADDRESS>
```

Usually on a machine that has it: `<PARENT_PROJECT_LOCAL_PATH>`

If you cannot reach it, say so and stop. Do not proceed on guesses.

This folder also sets rules of its own. Read `ASSETS.md` as well.
