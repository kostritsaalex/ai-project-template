I've read the procedure and the blueprint. Here's where things stand before the questions.

## Step 1 — Access

- **Framework:** the address you gave, `…/scratchpad/v3/fw`, is **outside this session's allowed working directories** — I couldn't read it. I read the same framework from `/home/kostritsaalex/Projects/Frameworks/ai-project-template`, which *is* allowed. Everything below comes from there. Tell me if that is not the same checkout as `v3/fw` and I'll stop.
- **Project folder:** reachable. ✅

## Step 2 — Look

1. **No `PROJECT.md`** in the folder. This is a fresh project scope.
2. Not applicable — this is a project scope, not a component.
3. **No other AI instruction files.** No `AGENTS.md`, `CLAUDE.md`, or anything from another framework. Nothing to overwrite.
4. **No parent claimed.** Correct for a project scope.
5. **The local path is a problem, and it is the one thing I can't settle for you.** The only form that resolves here is:

   ```
   /tmp/claude-1000/-home-kostritsaalex-Projects-Frameworks-ai-project-template/8a559101-7c29-4fe3-ac8b-f05b0b9eae34/scratchpad/v3/OneDrive/Projects/Test/northwind-crafts
   ```

   and it resolves only because `…/scratchpad/v3/OneDrive` is a **symlink** to `…/scratchpad/v3/store/OneDrive`. The natural form for the document, `~/OneDrive/Projects/Test/northwind-crafts`, **does not resolve on this machine** — I tested it. So I have a path that resolves but is true only inside this sandbox, and a path that belongs in the document but that I cannot verify. I'll bring this to you as a row in the summary table rather than guessing.
6. Not applicable — component only.

There's also no `.docs/` or `.docs/decisions/`; I'll state that in visible text as the blueprint requires.

## Step 3 — Blueprint

Read `blueprints/project/README.md` and `blueprints/project/PROJECT.md`, **Project Scope Blueprint 0.12.0** (framework 0.12.0), with `blueprints/setup/interview.md` at 0.13.0.

---

## Step 4 — The interview

1. **What it is.** What is this project about? What business does it represent, or what business tasks does it solve?

2. **Agent boundaries.** Is there anything an agent should never do in this project? For example: never set up deployment, never spend money on my behalf, never publish anything publicly. **If there is nothing, say so** — that is a complete answer and I will write it down as such.

3. **Principles.** The rules that hold across the whole project. Two are offered:

   - Validate ideas through practical use whenever possible.
   - Avoid speculative additions.

   Keep both, keep one, replace them with your own, or say none.

4. **Components.** Which additional folders and repositories belong to this project? Main project directory doesn't count. Naming one now does not set it up now — each is attached later with its own prompt, when you are ready.
