## Step 1. Access

| Location | Reachable |
| --- | --- |
| Framework | yes — `…/scratchpad/fw`, blueprints present |
| Project scope target | yes — `…/scratchpad/plain/pinewood-ceramics` |

Nothing unreachable.

## Step 2. Look

1. **`PROJECT.md` here?** No.
2. **Sent to attach a component into a project scope?** No — this is a project scope setup, so the clash does not apply.
3. **Other AI instruction files?** None, of this framework or any other. Nothing would be overwritten.
4. **Existing parent claim?** None.
5. **Does the local path resolve, in the form I intend to write it?** Yes. `/tmp/claude-1000/-home-kostritsaalex-Projects-Frameworks-ai-project-template/69e49674-1912-4b21-b875-255c577d6509/scratchpad/plain/pinewood-ceramics` resolves literally — `pwd -P` returns it unchanged, no symlink anywhere in the chain, and the filesystem is a plain `tmpfs` at `/tmp`. It is not under the home folder, so there is no `~` form to write. No symlink or mount arrangement holds it up, so there is no path note to write.
6. Not a component. Skipped.

## Step 3. Read the blueprint

Read `blueprints/project/README.md` (Blueprint Version 0.13.0, Framework Version 0.17.0) and `blueprints/project/PROJECT.md`, comments included, plus `blueprints/setup/interview.md` (Blueprint Version 0.13.0).

## Step 4. Ask

---

1. **What it is.** What is this project about? What business does it represent, or what business tasks does it solve?

2. **Agent boundaries.** Is there anything an agent should never do in this project? For example: never set up deployment, never spend money on my behalf, never publish anything publicly. **If there is nothing, say so** — that is a complete answer and I will write it down as such.

3. **Principles.** The rules that hold across the whole project. Two are offered:

   - Validate ideas through practical use whenever possible.
   - Avoid speculative additions.

   Keep both, keep one, replace them with your own, or say none.

4. **Components.** Which additional folders and repositories belong to this project? Main project directory doesn't count. Naming one now does not set it up now — each is attached later with its own prompt, when you are ready.

---

## Before the summary table: the address

Neither derivation rule fires. This folder is not a git working copy — there is no `.git` here or in any parent — and it is not inside a synced store; it sits on a local `tmpfs` under `/tmp`. So there is nothing to derive, and the fourth form in `0007` is not available to a project scope.

> This folder has no address yet, and a project needs one before I can set it up. Every folder you later add to this project keeps its own copy of this address, and that copy is how anyone working in one of those folders finds their way back here. So it has to be something that works from a different machine, rather than a path on this one.
>
> Two things count. A git remote you have pushed to, which gives an address like `https://github.com/you/northwind`. Or a folder inside a synced store — OneDrive, Google Drive, Dropbox — which gives one like `OneDrive, Projects/pinewood-ceramics`.
>
> `git init` on its own does not count. It gives this folder a history and leaves it in exactly one place, and what is being asked for is a second place.
>
> Set one of those up, or move the folder into one, and tell me when it is done. I will read the address from the folder rather than from your description. Until then there is nothing I can write, so setup stops here.

One note on how this interacts with your instruction to reach the Step 5 table: `interview.md` treats the address as the one value that ends the setup rather than arriving in the table as unknown. I am giving you the table anyway because you asked for it, with that row marked as the stop.

## Step 5. Summary

| Placeholder | Value I will write | Source |
| --- | --- | --- |
| `<PROJECT_NAME>` | Pinewood Ceramics | Proposed from the folder name `pinewood-ceramics`, spaces restored — correct it in a sentence if the business is called something else |
| `<PROJECT_PURPOSE>` | Pinewood Ceramics is a small ceramics business. It sells finished pieces, runs two exhibitions a year, and keeps the photography and the client briefs that go with them. Software is one part of it and not all of it. | Your answer 1, including the software line derived from it |
| `<AGENT_BOUNDARIES>` | Visible text: no work is currently forbidden in this project; an assistant meeting something which looks outside the project asks before starting it. Section kept. | Your answer 2, written in the form `interview.md` requires for "nothing" |
| `<PROJECT_PRINCIPLES>` | 1. Validate ideas through practical use whenever possible. 2. Avoid speculative additions. | Your answer 3 — both offered principles kept |
| `<PROJECT_SCOPE_ADDRESS>` | **unknown — setup stops here** | Neither rule fires: no `.git` in this folder or any parent, and the folder sits on local `tmpfs`, not in a synced store |
| `<PROJECT_LOCAL_PATH>` | `/tmp/claude-1000/-home-kostritsaalex-Projects-Frameworks-ai-project-template/69e49674-1912-4b21-b875-255c577d6509/scratchpad/plain/pinewood-ceramics` | Settled in Step 2: resolves in exactly this form, `pwd -P` unchanged. Not under the home folder, so no `~` form exists |
| `<PATH_NOTE>` | deleted | Settled from `namei -l` and `df`: no symlink in the chain, plain `tmpfs` at `/tmp`. The path holds on its own, so the blueprint says delete |
| `<SESSION_NOTE>` | deleted | Settled from your answer 4: no components, so no mount boundary for any of them to sit across. Adding a component elsewhere later brings this line back |
| `<COMPONENTS>` | Visible text: this project has no components; it lives in this one folder and the registry is empty. | Your answer 4, taken as complete |
| Project state | "This scope alone" — `PROJECT.md`, `AGENTS.md`, `CLAUDE.md` and nothing else. A finished setup, not half of one | Settled from `blueprints/project/README.md`, Scope section, against your answer 4 |
| `.docs/` and `.docs/decisions/` | Visible line saying neither folder exists yet | Settled from the folder: only `archive-2025/`, `client-briefs/`, `exhibition-prints/`, `glaze-tests/`, `supplier-notes/` are present |
| `<PROJECT_BLUEPRINT_VERSION>` | 0.13.0 | Settled from `blueprints/project/README.md` |
| `<YYYY-MM-DD>` | 2026-09-03 | Today's date |

No postures appear above because no components were named; the posture rows exist only per component block.

Stopping here as instructed. **No files written.**

Two things to hand back before you continue:

- **The address is the real blocker,** not a formality. Without it the three files cannot be written at all, and a second place is what you have to create — a pushed git remote, or moving this folder into OneDrive, Google Drive or Dropbox. Tell me when it is done and I will read the address off the folder.
- **A defect to report, per Step 4's instruction to report rather than patch:** `blueprints/setup/interview.md` declares `Framework Version: 0.13.0`, while `blueprints/project/README.md` declares `Framework Version: 0.17.0`. One of the two is stale. It changed nothing about the questions I asked.
