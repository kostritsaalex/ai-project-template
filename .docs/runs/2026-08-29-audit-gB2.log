All three addresses reachable: framework at `fw-v2`, blueprint library at `blueprints/`, target folder `sB/harbourline`.

**Step 2 — what the folder shows**

1. No `PROJECT.md` here.
2. Not applicable — this is a project scope, not a component attach.
3. No AI instruction files of any framework present (no `CLAUDE.md`, `AGENTS.md`, `COMPONENT.md`, `.cursorrules`, `GEMINI.md`). Nothing will be overwritten.
4. The folder claims no parent, and none was given.
5. The path resolves in exactly the form I intend to write it: `readlink -f` returns the same string, no symlink and no mount boundary — it sits directly on the `/tmp` tmpfs. So no path note is needed.
6. Also visible, and it settles the posture: the folder is a git working copy with remote `git@github.com:example-org/harbourline.git`, and `src/` holds one hand-written Python file. No vendored platform or framework core anywhere.

**Step 3** — read `blueprints/project/README.md`, Project Scope Blueprint **0.10.2** (framework 0.11.0), plus `PROJECT.md` with its comments.

**Step 5 — summary before anything is written**

| Placeholder | Value to be written | Source |
| --- | --- | --- |
| `<PROJECT_NAME>` | Harbourline | Folder name, confirmed by `README.md`. Proposed — correct it if the name carries spaces or different casing. |
| `<PROJECT_PURPOSE>` | A command-line tool that parses tide data and prints forecasts. The project is only software. | Your answer 1 |
| `<SCOPE_COVERS>` | "This project currently covers parsing tide data, the forecast maths, and the command line that exposes it. Anything else is outside it." | Your answer 2 |
| `<PROJECT_PRINCIPLES>` | 1. Validate ideas through practical use whenever possible. 2. Avoid speculative additions. | Your answer 3 — both defaults kept |
| `<PROJECT_SCOPE_ADDRESS>` | `https://github.com/example-org/harbourline` | Settled from `git remote -v` (`git@github.com:example-org/harbourline.git`), normalised to a URL with its scheme per decision 0007 |
| `<PROJECT_LOCAL_PATH>` | `/tmp/claude-1000/-home-kostritsaalex-Projects-Frameworks-ai-project-template/8a559101-7c29-4fe3-ac8b-f05b0b9eae34/scratchpad/sB/harbourline` | Resolved in Step 2, in this exact form. Not under `~`, so no tilde form exists. |
| `<PATH_NOTE>` | *deleted* | Settled from Step 2 item 5: the path holds on its own, no symlink or mount props it up |
| `<SESSION_NOTE>` | *deleted* | Settled: the one component sits inside this folder, so no filesystem boundary is crossed. Adding a component elsewhere brings the line back. |
| `<COMPONENTS>` | One block: **Harbourline Source** — `Assets. Live material. Work here as the task requires.` / `Address: src` | Folder from your answer 4. Name proposed from the project name. Posture proposed from Step 2 item 6 (own code, no vendored core). No `Local path:` line, because the component sits inside this folder. |
| Registry note | Visible line saying the component is listed but not yet wired — no stubs in `src/` yet | Settled from Step 2: `src/` holds no `COMPONENT.md`/`CLAUDE.md` |
| `.docs/` line | Visible line saying `.docs/` and `.docs/decisions/` do not exist yet | Settled from the folder listing |
| `<PROJECT_BLUEPRINT_VERSION>` | 0.10.2 | `blueprints/project/README.md` |
| `<YYYY-MM-DD>` | 2026-08-29 | Today |
| `<DOCUMENT_OWNER>` | Alex | Proposed from your account name — overturn in a word if it should read differently |

Two rows are worth a second look. **The posture**: the framework's test is whether a platform or framework updates the folder's code wholesale, and here it does not, so `Assets` is what that test yields. Decision 0006 says the two words behave identically in a folder of the project's own code with no vendored core, so this costs nothing today — say `Repository` and I'll write that instead, which is the better bet if you expect vendored dependencies to land in `src/` later. **The address**: `example-org` looks like a placeholder remote; if the real repository lives elsewhere, give me the URL.

Files this will write, all three at the root: `PROJECT.md`, `AGENTS.md`, `CLAUDE.md`. Nothing inside `src/` — its two stubs are a separate run of `new-component.md`, which I'll name again at hand-back.

Confirm the table, or overturn any row in a sentence, and I'll write.
