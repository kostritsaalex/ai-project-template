**Step 2 — what's already here**

1. No `PROJECT.md` in the folder. This is a fresh project scope.
2. Not applicable — no existing entry point to collide with.
3. No other AI instruction files, of this framework or another (`AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`: none).
4. The folder claims no parent.
5. The path `/tmp/claude-1000/-home-kostritsaalex-Projects-Frameworks-ai-project-template/8a559101-7c29-4fe3-ac8b-f05b0b9eae34/scratchpad/v12scope` resolves exactly as written — an ordinary directory on the local filesystem, no symlink and no mount holding it up. It is not under the home folder, so there is no `~/` form of it. No `.git` anywhere in the tree and no synced store above it.
6. Not applicable — this is a project scope, not a component.

**Step 3 — blueprint read**

`blueprints/project/README.md` and `blueprints/project/PROJECT.md`, Blueprint Version 0.12.0 / Framework Version 0.12.0, comments included. Interview text from `blueprints/setup/interview.md`, same version.

**Step 5 — summary**

| Placeholder | Value to be written | Source |
| --- | --- | --- |
| `<PROJECT_NAME>` | `v12scope` | Folder name, offered as a **draft**. This one wants correcting — the name keeps its spaces and the folder need not match, so something like `Northwind Furniture Workshop` is a sentence away. |
| `<PROJECT_PURPOSE>` | A small furniture workshop. It restores mid-century pieces, photographs them and sells them through a shop site. Software is one part of it, not all of it. | Your answer 1 |
| `<SCOPE_COVERS>` | `restoration, photography and selling online` — written as *"This project currently covers restoration, photography and selling online. Anything else is outside it."* | Your answer 2 |
| `<PROJECT_PRINCIPLES>` | Validate ideas through practical use whenever possible. / Avoid speculative additions. | Your answer 3 — both offered defaults kept |
| `<PROJECT_SCOPE_ADDRESS>` | `none. No copy of this folder exists off this machine.` | Settled: no git remote in or above the folder, and the path resolves under `/tmp`, not inside a synced store. Neither derivation rule yields a value, so decision `0007`'s fourth form applies. |
| `<PROJECT_LOCAL_PATH>` | `/tmp/claude-1000/-home-kostritsaalex-Projects-Frameworks-ai-project-template/8a559101-7c29-4fe3-ac8b-f05b0b9eae34/scratchpad/v12scope` | Settled: resolved in Step 2 in this exact form. Absolute rather than `~/`-relative because the folder is not under the home folder. |
| `<PATH_NOTE>` | *deleted* | Settled: the path holds on its own — a plain directory, no symlink, no mount. |
| `<SESSION_NOTE>` | *deleted*, with a visible line saying a component added elsewhere brings it back | Settled: both components sit inside this folder, so there is no boundary for a session to land on the wrong side of. |
| `<COMPONENTS>` | Two blocks:<br>**storefront** — `Repository. Things get changed here. Platform or framework core changes only through its own update mechanism, never by hand.` Address: `storefront`<br>**lookbook** — `Assets. Live material. Work here as the task requires.` Address: `lookbook` | Names from your answer 4. Postures **proposed**: `storefront` holds `package.json` and `src/`, so platform/framework code an updater replaces wholesale; `lookbook` holds photographs, which is live material. Local path omitted on both — the address is relative to this folder and adds the rest. |
| `<PROJECT_BLUEPRINT_VERSION>` | `0.12.0` | Blueprint file read in Step 3 |
| `<YYYY-MM-DD>` | `2026-08-30` | Today |
| Documentation and decisions | Visible line: `.docs/` and `.docs/decisions/` do not exist yet. | Settled: neither is in the folder. |

Two things the table does not cover, so they are here instead. `notes/` sits in the root and you did not declare it, so it stays files in the project scope and gets no block — say the word if you meant it as a component. And both components are registry entries only until their stubs are written: after this, `new-component.md` runs once for `storefront` and once for `lookbook`.

Nothing written. Confirm or overturn any row and I'll proceed to Step 6.
