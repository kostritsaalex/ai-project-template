I could not reach the framework at the path in your prompt — `…/scratchpad/v3/fw` is outside this session's allowed directories. I read the blueprints from the checkout at `/home/kostritsaalex/Projects/Frameworks/ai-project-template` instead. Tell me if that is not the same library and I will stop and re-read. The project folder itself is reachable.

**What I found by looking:**

1. No `PROJECT.md` here.
2. Not applicable — this is a project scope, not a component being attached.
3. No other AI instruction files, of this framework or another (no `CLAUDE.md`, `AGENTS.md`, `COMPONENT.md`, `.cursorrules`, `GEMINI.md` anywhere in the top three levels).
4. The folder claims no parent.
5. The path resolves, in the form given. It holds because of a symlink: `…/scratchpad/v3/OneDrive` points to `…/scratchpad/v3/store/OneDrive`. Without that link the written path is false. There is also no `.git` in this folder, so the address comes from the synced store rather than from a remote.

Read: `blueprints/project/README.md` (Blueprint Version 0.12.0) with `blueprints/project/PROJECT.md`, and `blueprints/setup/interview.md` (Blueprint Version 0.13.0).

---

1. **What it is.** What is this project about? What business does it represent, or what business tasks does it solve?

2. **Agent boundaries.** Is there anything an agent should never do in this project? For example: never set up deployment, never spend money on my behalf, never publish anything publicly. **If there is nothing, say so** — that is a complete answer and I will write it down as such.

3. **Principles.** The rules that hold across the whole project. Two are offered:

   - Validate ideas through practical use whenever possible.
   - Avoid speculative additions.

   Keep both, keep one, replace them with your own, or say none.

4. **Components.** Which additional folders and repositories belong to this project? Main project directory doesn't count. Naming one now does not set it up now — each is attached later with its own prompt, when you are ready.
